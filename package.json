{
  "name": "search-card-tests",
  "private": true,
  "scripts": {
    "test": "playwright test -c tests/web/playwright.config.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.58.2",
    "esbuild": "0.25.12",
    "lit": "3.3.1"
  }
}
